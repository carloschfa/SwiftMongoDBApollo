mutation UpdateObject($objectId: String!,
                      $text: String!,
                      $number: Int!,
                      $boolean: Boolean!,
                      $updatedAt: String!) {
  
  updateObject(objectId: $objectId,
               text: $text,
               number: $number,
               boolean: $boolean,
               updatedAt: $updatedAt) {
    success
    message
  }
}

