mutation InsertObject($objectId: String!,
                      $category: String!,
                      $text: String!,
                      $number: Int!,
                      $boolean: Boolean!,
                      $createdAt: String!) {
  
  insertObject(objectId: $objectId,
              category: $category,
              text: $text,
              number: $number,
              boolean: $boolean,
              createdAt: $createdAt) {
    success
    message
  }
}

